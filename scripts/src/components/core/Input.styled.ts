import styled from "styled-components";

export const Input = styled.input`
    max-width: 300px;
    outline: none;
    border: 1px solid #dbdbdb;
    border-radius: 4px;
    padding: 4px;
    position: relative;
`;
