import React from "react";
import { RoundBlock } from "@src/components/core";
import * as s from "./Auth.styled";

type Props = {};

export const Auth: React.FC<Props> = ({}) => {
    return (
        <s.Wrapper>
            <RoundBlock>12341 24124</RoundBlock>
        </s.Wrapper>
    );
};
