import React from "react";
import * as s from "./Header.styled";

type Props = {};

export const Header: React.FC<Props> = ({}) => {
    return (
        <s.Wrapper>
            <s.Content>LOGO SOMESHIT PROFILE</s.Content>
        </s.Wrapper>
    );
};
